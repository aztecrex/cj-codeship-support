bash:
  build:
    path: src/bash
    dockerfile_path: Dockerfile
    image: cjengineering/codeship-bash

aws_base:
  build:
    path: src/aws-base
    dockerfile_path: Dockerfile
    image: cjentineering/codeship-aws-base

aws_cli:
  build: ./src/aws-cli

aws_ecs_deploy:
  build: ./src/aws-ecs-deploy

aws_ecs_run:
  build: ./src/aws-ecs-run

aws_s3_deploy:
  build: ./src/aws-s3-deploy

aws_docker:
  build: ./src/aws-docker

git:
  build: ./src/git
