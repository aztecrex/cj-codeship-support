aws_base:
  build:
    image: cjengineering/codeship-aws-base
    path: src/aws-base
    dockerfile_path: Dockerfile
aws_cli:
  build: ./src/aws-cli

aws_ecs_deploy:
  build: ./src/aws-ecs-deploy

aws_s3_deploy:
  build: ./src/aws-s3-deploy

aws_s3_run:
  build: ./src/aws-s3-run

git:
  build: ./src/git
