bash:
  build: src/aws-base

aws_base:
  build: src/aws-base

aws_cli:
  build: ./src/aws-cli

aws_ecs_deploy:
  build: ./src/aws-ecs-deploy

aws_ecs_run:
  build: ./src/aws-ecs-run

aws_s3_deploy:
  build: ./src/aws-s3-deploy

aws_docker:
  build: ./src/aws-docker

git:
  build: ./src/git
